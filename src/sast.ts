/**
 * A module for defining the nodes in an executable
 * document. In the future, this is likely to be
 * Typescript generated from JSON Schema in our schema repo.
 */

/**
 * A node in an executable document.
 */
export type Node = {[key: string]: any}
