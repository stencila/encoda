List of characters in Star Wars from SWAPI, the Star Wars API, http://swapi.co/ via Hadley Wickham's `dplyr` package

```r
library(dplyr)
write.csv(starwars[1:10],file='starwars-people.csv', row.names=F, quote=F)
```
